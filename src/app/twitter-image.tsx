// m27 (Phase 2): Twitter card image, auto-discovered by Next 13+ App
// Router. Re-uses the same renderer as opengraph-image so the two
// social channels share the brand artwork without duplicating layout
// code. Twitter's `summary_large_image` card uses 1200×630.
export { default, alt, size, contentType } from './opengraph-image'
